# 授权系统手动测试清单

## v7.141.3 真实用户认证集成验证

**测试日期**: ___________
**测试人员**: ___________
**版本**: v7.141.3+

---

## 一、前置准备

### 1.1 环境检查
- [ ] 后端服务正常运行 (`http://localhost:8000`)
- [ ] 前端服务正常运行 (`http://localhost:3000`)
- [ ] WordPress API 可访问 (`https://www.ucppt.com`)
- [ ] Milvus 服务正常运行
- [ ] 浏览器开发者工具已打开（F12）

### 1.2 测试账号准备
- [ ] **免费用户账号** (VIP Level = 0)
  - 用户名: ___________
  - 密码: ___________

- [ ] **专业版用户账号** (VIP Level = 2)
  - 用户名: ___________
  - 密码: ___________

- [ ] **管理员账号** (可选)
  - 用户名: ___________
  - 密码: ___________

### 1.3 测试文件准备
```bash
# 创建测试文件
python -c "with open('test_5mb.txt', 'w') as f: f.write('A' * (5 * 1024 * 1024))"
python -c "with open('test_6mb.txt', 'w') as f: f.write('B' * (6 * 1024 * 1024))"
python -c "with open('test_40mb.txt', 'w') as f: f.write('C' * (40 * 1024 * 1024))"
```

- [ ] `test_5mb.txt` (5MB) 已创建
- [ ] `test_6mb.txt` (6MB) 已创建
- [ ] `test_40mb.txt` (40MB) 已创建

---

## 二、认证状态测试

### 测试 2.1: 未登录用户访问控制

**步骤**:
1. 清除浏览器 localStorage: `localStorage.clear()`
2. 访问 `http://localhost:3000/admin/knowledge-base`

**预期结果**:
- [ ] 自动跳转到登录页面 **或**
- [ ] 显示登录提示界面
- [ ] Console 无严重错误

**实际结果**:
___________________________________________________________

**状态**: ✅ 通过 / ❌ 失败

---

### 测试 2.2: 已登录用户自动获取会员信息

**步骤**:
1. 登录免费用户账号
2. 访问 `http://localhost:3000/admin/knowledge-base`
3. 打开 Console，查看日志

**预期结果**:
- [ ] Console 输出: `[useMembership] 获取会员信息...`
- [ ] Console 输出: `tier = "free"`
- [ ] 页面正常加载，无错误提示

**实际结果**:
___________________________________________________________

**状态**: ✅ 通过 / ❌ 失败

---

## 三、会员等级映射测试

### 测试 3.1: 免费用户 (VIP Level 0 → free)

**步骤**:
1. 登录免费用户
2. 打开 Console
3. 执行: `localStorage.getItem('wp_jwt_user')`
4. 查看 `membership_level` 字段

**预期结果**:
- [ ] `membership_level = 0` 或未设置
- [ ] 后端接收到 `user_tier = "free"`

**检查方法**:
```javascript
// 在 Console 执行
fetch('/api/member/my-membership', {
  headers: { 'Authorization': `Bearer ${localStorage.getItem('wp_jwt_token')}` }
}).then(r => r.json()).then(d => console.log('Membership:', d))
```

**实际结果**:
___________________________________________________________

**状态**: ✅ 通过 / ❌ 失败

---

### 测试 3.2: 专业版用户 (VIP Level 2 → professional)

**步骤**:
1. 登录专业版用户
2. 刷新页面
3. 打开 Console，执行上述检查方法

**预期结果**:
- [ ] `level = 2`
- [ ] `level_name = "超级会员"`
- [ ] 前端映射为 `tier = "professional"`

**实际结果**:
___________________________________________________________

**状态**: ✅ 通过 / ❌ 失败

---

## 四、配额检查集成测试

### 测试 4.1: 免费用户上传 5MB 文件（应成功）

**步骤**:
1. 登录免费用户
2. 访问知识库管理页面
3. 切换到"数据导入"标签
4. 选择"私有知识库"
5. 选择 `test_5mb.txt` 文件

**预期结果**:
- [ ] 显示 "⏳ 正在检查配额..."
- [ ] 显示 "✅ 配额检查通过"
- [ ] 上传按钮可用
- [ ] 点击上传成功

**实际结果**:
___________________________________________________________

**状态**: ✅ 通过 / ❌ 失败

---

### 测试 4.2: 免费用户上传 6MB 文件（应失败）

**步骤**:
1. 继续使用免费用户
2. 选择 `test_6mb.txt` 文件

**预期结果**:
- [ ] Toast 错误提示: "⚠️ 无法上传"
- [ ] 错误信息: "文件大小超过限制 (6.0/5 MB)"
- [ ] 上传按钮禁用，显示"配额不足"
- [ ] 建议: "升级会员等级以提升单文件大小限制"

**实际结果**:
___________________________________________________________

**状态**: ✅ 通过 / ❌ 失败

---

### 测试 4.3: 专业版用户上传 40MB 文件（应成功）

**步骤**:
1. 退出免费用户，登录专业版用户
2. 选择 `test_40mb.txt` 文件

**预期结果**:
- [ ] 配额检查通过
- [ ] 文件成功上传
- [ ] Toast 成功提示: "成功导入 1 个文档"

**实际结果**:
___________________________________________________________

**状态**: ✅ 通过 / ❌ 失败

---

## 五、真实用户ID验证测试

### 测试 5.1: 验证后端接收真实用户ID

**步骤**:
1. 登录任意用户
2. 打开 Network 标签
3. 选择小文件（< 5MB）
4. 点击"开始上传"
5. 查看 `/api/admin/milvus/import/file` 请求

**预期结果**:
- [ ] Request Payload 中 `owner_id` 不是 `"user_mock_123"`
- [ ] `owner_id` 是真实的用户ID（如 `"123"`）
- [ ] `user_tier` 是真实的会员等级（如 `"free"`, `"professional"`）

**实际结果**:
```
owner_id: ___________
user_tier: ___________
```

**状态**: ✅ 通过 / ❌ 失败

---

### 测试 5.2: 验证配额检查API使用真实数据

**步骤**:
1. 选择文件后，查看 Network
2. 找到 `/api/admin/milvus/quota/check` 请求
3. 查看查询参数

**预期结果**:
- [ ] `user_id` 不是 `"user_mock_123"`
- [ ] `user_tier` 不是硬编码的 `"free"`
- [ ] 参数与当前登录用户匹配

**实际结果**:
```
user_id: ___________
user_tier: ___________
```

**状态**: ✅ 通过 / ❌ 失败

---

## 六、错误容错测试

### 测试 6.1: 会员API失败时的降级

**步骤**:
1. 打开 Network 标签
2. 右键 → Block request URL pattern
3. 添加规则: `*/api/member/my-membership*`
4. 刷新页面

**预期结果**:
- [ ] Console 错误日志: `[useMembership] Error fetching membership`
- [ ] 自动降级为免费层级 (`tier = "free"`)
- [ ] 系统仍可使用
- [ ] 上传限制按免费层级执行（5MB）

**实际结果**:
___________________________________________________________

**状态**: ✅ 通过 / ❌ 失败

---

### 测试 6.2: WordPress API超时处理

**步骤**:
1. 使用浏览器扩展（如 ModHeader）设置网络延迟
2. 或临时关闭 WordPress 服务
3. 刷新页面

**预期结果**:
- [ ] 请求超时后降级为免费层级
- [ ] 页面不崩溃
- [ ] 显示友好的错误提示（可选）

**实际结果**:
___________________________________________________________

**状态**: ✅ 通过 / ❌ 失败

---

## 七、跨浏览器兼容性测试

### 测试 7.1: Chrome

- [ ] 认证正常
- [ ] 会员信息获取正常
- [ ] 配额检查正常
- [ ] 文件上传正常

### 测试 7.2: Firefox

- [ ] 认证正常
- [ ] 会员信息获取正常
- [ ] 配额检查正常
- [ ] 文件上传正常

### 测试 7.3: Edge

- [ ] 认证正常
- [ ] 会员信息获取正常
- [ ] 配额检查正常
- [ ] 文件上传正常

---

## 八、性能测试

### 测试 8.1: 会员信息获取耗时

**步骤**:
1. 打开 Console
2. 执行:
```javascript
console.time('membership');
fetch('/api/member/my-membership', {
  headers: { 'Authorization': `Bearer ${localStorage.getItem('wp_jwt_token')}` }
}).then(r => r.json()).then(d => {
  console.timeEnd('membership');
  console.log(d);
});
```

**预期结果**:
- [ ] 响应时间 < 1秒

**实际耗时**: ___________ ms

**状态**: ✅ 通过 / ❌ 失败

---

### 测试 8.2: 配额检查耗时

**步骤**:
1. 选择文件
2. 观察 Network 中 `/quota/check` 请求耗时

**预期结果**:
- [ ] 响应时间 < 500ms

**实际耗时**: ___________ ms

**状态**: ✅ 通过 / ❌ 失败

---

## 九、测试总结

### 通过率统计

- **总测试项**: 18
- **通过**: _____
- **失败**: _____
- **通过率**: _____%

### 关键问题记录

| 序号 | 测试项 | 问题描述 | 严重程度 | 状态 |
|------|--------|----------|----------|------|
| 1    |        |          | 🔴高/🟡中/🟢低 |      |
| 2    |        |          |          |      |
| 3    |        |          |          |      |

### 测试结论

- [ ] ✅ **所有测试通过** - 系统可以投入使用
- [ ] ⚠️ **部分测试失败** - 需要修复后重新测试
- [ ] ❌ **多项测试失败** - 需要全面排查和修复

### 后续行动

1. ___________________________________________________________
2. ___________________________________________________________
3. ___________________________________________________________

---

**测试完成日期**: ___________
**测试人员签名**: ___________
