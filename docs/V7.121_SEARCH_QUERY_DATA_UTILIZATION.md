# v7.121 搜索查询数据利用增强

**实施日期**: 2026-01-03
**版本**: v7.121
**类型**: Enhancement (功能增强)
**优先级**: 高

## 概述

增强搜索查询生成功能，充分利用用户原始输入和问卷摘要数据，提升搜索查询的精准度和个性化程度。

## 背景

### 问题发现

在 v7.120 完成搜索引用 WebSocket 推送修复后，对搜索工具数据利用情况进行复核，发现：

**当前数据利用情况**:
- ✅ **已使用**: deliverable_name, deliverable_description, keywords, constraints, project_task
- ❌ **未使用**: user_input (用户原始问题), questionnaire_summary (问卷精炼摘要)

**影响**:
- 搜索查询缺少用户的个性化标签和偏好
- 搜索查询缺少用户的情感需求和价值观
- 搜索结果相关性不够精准

### 示例对比

**优化前的查询**（仅基于交付物+项目任务）:
```
1. "用户画像 设计案例 2024"
2. "独立女性 现代简约 best practices"
3. "构建目标用户详细画像 研究资料"
```

**优化后的查询**（整合用户输入+问卷）:
```
1. "用户画像 现代简约 优雅 设计案例 2024"
2. "独立女性 归属感 自由度"
3. "我是海归女性，喜欢Audrey Hepburn优雅风格，需要一个有归属感的私密空间 研究资料"
```

**差异**:
- ✅ 包含用户的个性化标签（海归、Audrey Hepburn风格）
- ✅ 包含情感需求（归属感、自由度、私密空间）
- ✅ 包含用户行为特征（年轻白领的生活方式）

## 修改内容

### 1. search_strategy.py - 扩展方法签名

**文件**: `intelligent_project_analyzer/agents/search_strategy.py`

#### 修改位置: Lines 28-38

**修改前**:
```python
def generate_deliverable_queries(
    self,
    deliverable_name: str,
    deliverable_description: str = "",
    keywords: list = None,
    constraints: dict = None,
    project_task: str = "",
    num_queries: int = 3
) -> list:
```

**修改后**:
```python
def generate_deliverable_queries(
    self,
    deliverable_name: str,
    deliverable_description: str = "",
    keywords: list = None,
    constraints: dict = None,
    project_task: str = "",
    user_input: str = "",  # 🔥 v7.121: 新增 - 用户原始问题
    questionnaire_summary: dict = None,  # 🔥 v7.121: 新增 - 问卷精炼摘要
    num_queries: int = 3
) -> list:
```

#### 修改位置: Lines 58-87 - 增强降级方案

**修改前**:
```python
# 降级方案：生成基于模板的查询
queries = []

# 查询1: 基于交付物名称
queries.append(f"{deliverable_name} 设计案例 2024")

# 查询2: 基于关键词
if keywords:
    keywords_str = " ".join(keywords[:2])
    queries.append(f"{keywords_str} best practices")

# 查询3: 基于描述
if deliverable_description:
    desc_short = deliverable_description[:30].replace("\n", " ").strip()
    queries.append(f"{desc_short} 研究资料")
else:
    queries.append(f"{deliverable_name} 研究资料")
```

**修改后**:
```python
# 🔥 v7.121: 降级方案 - 生成基于模板的查询（整合用户数据）
queries = []

# 从问卷摘要中提取关键信息
style_keywords = []
emotion_keywords = []
if questionnaire_summary:
    style_keywords = questionnaire_summary.get("style_preferences", [])[:2]
    emotion_keywords = questionnaire_summary.get("emotional_requirements", [])[:2]

# 查询1: 交付物 + 风格偏好
style_str = " ".join(style_keywords) if style_keywords else ""
queries.append(f"{deliverable_name} {style_str} 设计案例 2024".strip())

# 查询2: 关键词 + 情感需求
keywords_str = " ".join(keywords[:2]) if keywords else deliverable_name
emotion_str = " ".join(emotion_keywords) if emotion_keywords else "best practices"
queries.append(f"{keywords_str} {emotion_str}".strip())

# 查询3: 描述 + 用户原始需求
if deliverable_description:
    desc_short = deliverable_description[:30].replace("\n", " ").strip()
    queries.append(f"{desc_short} 研究资料")
elif user_input and len(user_input) > 20:
    # 从用户输入中提取关键短语
    user_short = user_input[:50].replace("\n", " ").strip()
    queries.append(f"{user_short} 研究资料")
else:
    queries.append(f"{deliverable_name} 研究资料")
```

#### 修改位置: Lines 108-160 - 增强LLM prompt

**修改前的方法签名**:
```python
def _generate_queries_with_llm(
    self,
    deliverable_name: str,
    deliverable_description: str,
    keywords: list,
    project_task: str,
    num_queries: int
) -> list:
```

**修改后的方法签名**:
```python
def _generate_queries_with_llm(
    self,
    deliverable_name: str,
    deliverable_description: str,
    keywords: list,
    project_task: str,
    user_input: str = "",  # 🔥 v7.121: 新增参数
    questionnaire_summary: dict = None,  # 🔥 v7.121: 新增参数
    num_queries: int = 3
) -> list:
```

**修改后的prompt构建逻辑**:
```python
# 🔥 v7.121: 构建问卷摘要文本
questionnaire_text = ""
if questionnaire_summary:
    style_prefs = questionnaire_summary.get("style_preferences", [])
    functional_needs = questionnaire_summary.get("functional_requirements", [])
    emotional_needs = questionnaire_summary.get("emotional_requirements", [])

    if style_prefs or functional_needs or emotional_needs:
        questionnaire_text = "\n用户偏好（来自问卷）:\n"
        if style_prefs:
            questionnaire_text += f"- 风格偏好: {', '.join(style_prefs)}\n"
        if functional_needs:
            questionnaire_text += f"- 功能需求: {', '.join(functional_needs)}\n"
        if emotional_needs:
            questionnaire_text += f"- 情感需求: {', '.join(emotional_needs)}\n"

# 🔥 v7.121: 用户原始问题（提取关键部分）
user_context = ""
if user_input and len(user_input) > 20:
    user_context = f"\n用户原始需求:\n{user_input[:300]}\n"

prompt = f"""基于以下信息，为设计交付物生成 {num_queries} 个高质量的搜索查询。

交付物名称: {deliverable_name}
交付物描述: {deliverable_description}
关键词: {', '.join(keywords) if keywords else '无'}
项目背景: {project_task[:200]}
{user_context}
{questionnaire_text}

要求：
1. 每个查询应该精准、具体，充分反映用户的个性化需求
2. 优先整合用户偏好和情感需求到查询中
3. 适合在设计案例网站、学术资料库搜索
4. 结合中英文关键词
5. 包含年份信息（2023-2024）

以JSON数组格式输出，例如：
["查询1", "查询2", "查询3"]
"""
```

### 2. search_query_generator_node.py - 传递完整数据

**文件**: `intelligent_project_analyzer/workflow/nodes/search_query_generator_node.py`

#### 修改位置: Lines 74-86

**添加的代码**:
```python
# 🔥 v7.121: 提取问卷摘要和用户输入
questionnaire_summary = state.get("questionnaire_summary", {})
user_input_full = state.get("user_input", "")

logger.debug(f"📋 [搜索查询生成] 可用数据:")
logger.debug(f"   - 用户输入长度: {len(user_input_full)} 字符")
logger.debug(f"   - 问卷摘要: {bool(questionnaire_summary)}")
if questionnaire_summary:
    logger.debug(f"   - 问卷内容: {list(questionnaire_summary.keys())}")
```

#### 修改位置: Lines 99-109

**修改前**:
```python
search_queries = search_generator.generate_deliverable_queries(
    deliverable_name=deliverable_name,
    deliverable_description=deliverable_description,
    keywords=keywords,
    constraints=constraints,
    project_task=project_task,
    num_queries=3
)
```

**修改后**:
```python
# 🔥 v7.121: 传递完整数据（包含用户输入和问卷摘要）
search_queries = search_generator.generate_deliverable_queries(
    deliverable_name=deliverable_name,
    deliverable_description=deliverable_description,
    keywords=keywords,
    constraints=constraints,
    project_task=project_task,
    user_input=user_input_full,  # 🔥 传递完整用户输入
    questionnaire_summary=questionnaire_summary,  # 🔥 传递问卷摘要
    num_queries=3
)
```

## 测试验证

### 单元测试

**文件**: `tests/test_search_query_data_utilization.py`

创建了 5 个测试用例：

1. ✅ **test_generate_queries_with_user_input** - 验证用户输入整合
2. ✅ **test_generate_queries_with_questionnaire_summary** - 验证问卷摘要整合
3. ✅ **test_generate_queries_with_both_user_and_questionnaire** - 验证双重数据整合
4. ✅ **test_fallback_without_user_data** - 验证降级方案
5. ✅ **test_questionnaire_summary_structure** - 验证问卷摘要结构兼容性

### 测试结果

```bash
pytest tests/test_search_query_data_utilization.py -v

============================= test session starts =============================
platform win32 -- Python 3.13.5, pytest-9.0.2, pluggy-1.5.0
collected 5 items

tests/test_search_query_data_utilization.py::test_generate_queries_with_user_input PASSED
tests/test_search_query_data_utilization.py::test_generate_queries_with_questionnaire_summary PASSED
tests/test_search_query_data_utilization.py::test_generate_queries_with_both_user_and_questionnaire PASSED
tests/test_search_query_data_utilization.py::test_fallback_without_user_data PASSED
tests/test_search_query_data_utilization.py::test_questionnaire_summary_structure PASSED

============================== 5 passed in 0.31s ==============================
```

## 技术细节

### 数据流

```
用户输入分析
    │
    ├─→ user_input (原始问题)
    │      ↓
    └─→ questionnaire_summary (问卷摘要)
           │
           ├─ style_preferences (风格偏好)
           ├─ functional_requirements (功能需求)
           └─ emotional_requirements (情感需求)
                     ↓
        search_query_generator_node
                     ↓
        SearchStrategyGenerator
                     ↓
        enhanced search queries (增强查询)
```

### Questionnaire Summary 结构

```python
{
    "style_preferences": ["现代简约", "优雅", "北欧风"],
    "functional_requirements": ["私密空间", "工作区域", "开放式厨房"],
    "emotional_requirements": ["归属感", "自由度", "舒适感"]
}
```

### Token 控制策略

为避免 LLM prompt 过长：
- **user_input**: 截取前 300 字符
- **project_task**: 截取前 200 字符
- **questionnaire_summary**: 仅提取 3 个关键字段，每个字段取前 N 项
- **总 prompt 长度**: 控制在 1000 tokens 内

## 向后兼容性

✅ **完全向后兼容**

- 新参数 `user_input` 和 `questionnaire_summary` 为可选参数（默认为空）
- 不传递时自动使用降级方案
- 现有调用代码无需修改即可正常工作
- 仅在提供了额外数据时才会利用增强逻辑

## 预期效果

### 搜索查询质量提升

**示例场景**: 用户输入 = "我是一位海归独立女性，喜欢Audrey Hepburn那种优雅简约的风格..."

**优化前**:
```
1. "用户画像 设计案例 2024"
2. "独立女性 best practices"
3. "构建目标用户详细画像 研究资料"
```

**优化后**:
```
1. "用户画像 现代简约 优雅 设计案例 2024"
2. "独立女性 归属感 自由度"
3. "我是海归女性，喜欢Audrey Hepburn优雅风格 研究资料"
```

### 搜索结果相关性提升

- ✅ 更精准匹配用户个性化需求
- ✅ 包含用户的情感诉求和价值观
- ✅ 反映用户的生活方式和审美偏好
- ✅ 提高搜索结果的适用性和启发性

## 修改文件清单

| 文件 | 修改类型 | 行数 |
|------|---------|------|
| `intelligent_project_analyzer/agents/search_strategy.py` | 扩展方法签名 | 28-38 |
| | 增强降级方案 | 58-87 |
| | 增强LLM prompt | 108-160 |
| `intelligent_project_analyzer/workflow/nodes/search_query_generator_node.py` | 提取问卷数据 | 74-86 |
| | 传递完整数据 | 99-109 |
| `tests/test_search_query_data_utilization.py` | 新增单元测试 | 1-169 (新文件) |

## 后续优化建议

### 短期优化

1. **动态权重调整**: 根据问卷完整度调整数据权重
2. **A/B 测试**: 对比优化前后的搜索结果相关性
3. **用户反馈**: 收集专家对搜索结果质量的评价

### 长期优化

1. **智能摘要**: 使用 LLM 自动提取问卷摘要的关键信息
2. **查询优化器**: 基于搜索结果反馈自动调优查询生成策略
3. **个性化模型**: 为不同用户类型训练专门的查询生成模型

## 已知限制

1. **Questionnaire Summary 依赖**: 需要问卷系统返回标准化的摘要结构
2. **Token 限制**: user_input 和 questionnaire_summary 过长时会被截断
3. **中文支持**: LLM 需要支持中文才能充分利用中文用户数据

## 相关文档

### 搜索功能文档

- [v7.120 搜索引用功能发布说明](V7.120_SEARCH_REFERENCES_RELEASE.md)
- [v7.120 WebSocket推送修复](../.github/V7.120_SEARCH_REFERENCES_WEBSOCKET_FIX.md)
- [搜索工具诊断报告](../SEARCH_TOOLS_DIAGNOSTIC_REPORT.md)

### 代码参考

**核心文件**:
- `intelligent_project_analyzer/agents/search_strategy.py` - 搜索策略生成器
- `intelligent_project_analyzer/workflow/nodes/search_query_generator_node.py` - 搜索查询生成节点
- `intelligent_project_analyzer/core/state.py` - State 定义（包含 questionnaire_summary 字段）

**测试文件**:
- `tests/test_search_query_data_utilization.py` - 数据利用测试

---

**版本**: v7.121
**实施日期**: 2026-01-03
**实施状态**: ✅ 完成
**测试状态**: ✅ 通过（5/5 tests）
**向后兼容**: ✅ 完全兼容
**风险等级**: 低（新增可选参数，不影响现有功能）
