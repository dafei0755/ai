# 问卷核心任务编辑功能实施报告 v7.111

## 📋 实施目标

实现问卷Step 1的核心任务编辑功能，让用户可以修改、删除、新增AI识别的核心任务。

---

## ✅ 完成功能

### 1. 前端任务编辑UI

**文件**: `frontend-nextjs/components/UnifiedProgressiveQuestionnaireModal.tsx`

#### 新增功能：

1. **任务编辑状态管理**
   ```typescript
   interface EditableTask {
     id?: string;
     title: string;
     description: string;
     source_keywords?: string[];
     isEditing?: boolean;    // 是否编辑中
     isNew?: boolean;        // 是否新增任务
   }

   const [editedTasks, setEditedTasks] = useState<EditableTask[]>([]);
   const [originalTasksCount, setOriginalTasksCount] = useState(0);
   ```

2. **草稿缓存机制**
   - 缓存键: `questionnaire-tasks-draft-${sessionId}`
   - 有效期: 1小时
   - 自动保存: 任务状态变化时自动保存
   - 自动恢复: 页面刷新后恢复编辑状态

3. **任务编辑操作**
   - **编辑**: `handleEditTask(index)` - 进入编辑模式
   - **保存**: `handleSaveTask(index)` - 验证并保存任务
   - **取消**: `handleCancelEdit(index)` - 恢复原始数据或删除未完成的新任务
   - **删除**: `handleDeleteTask(index)` - 确认删除任务（最少保留1个）
   - **新增**: `handleAddTask()` - 添加新任务（最多15个）

4. **字段验证规则**
   - 任务标题: ≥5个字符
   - 任务描述: ≥20个字符
   - 任务数量: 1-15个

5. **任务状态栏**
   ```
   原始任务数: 3 | 当前任务数: 5 | 新增: 2 | 删除: 0
   可添加 10 个任务
   ```

6. **视觉设计特性**
   - 新增任务: 左侧绿色竖线 + "新增"徽章
   - 编辑模式: 蓝色聚焦环 + 内联表单
   - 查看模式: hover显示编辑/删除按钮
   - 字数实时统计: 标题(5/5) 描述(20/20)

7. **确认按钮动态化**
   - 无修改: "确认任务列表"
   - 有修改: "保存修改并继续（3处修改）"

### 2. 后端任务接收处理

**文件**: `intelligent_project_analyzer/interaction/nodes/progressive_questionnaire.py`

#### 修改内容：

1. **用户响应解析增强** (L160-189)
   ```python
   user_modified = False  # 🆕 v7.111: 标记用户是否修改了任务

   if isinstance(user_response, dict):
       if "extracted_tasks" in user_response:
           confirmed_tasks = user_response["extracted_tasks"]
           user_modified = True
           logger.info(f"✏️ [v7.111] 用户修改了任务列表，共 {len(confirmed_tasks)} 个任务")
   ```

2. **State字段新增** (L252-266)
   ```python
   update_dict = {
       "confirmed_core_tasks": confirmed_tasks,  # 用户确认/修改后的任务
       "user_modified_tasks": user_modified,     # 🆕 标志位
       ...
   }
   ```

3. **兼容性保证**
   - 支持新格式: `extracted_tasks`（用户编辑）
   - 支持旧格式: `confirmed_tasks`, `tasks`（兼容）

### 3. 前端API调用适配

**文件**: `frontend-nextjs/app/analysis/[sessionId]/page.tsx`

#### 修改内容：

```typescript
// 🆕 v7.111: 支持任务编辑参数传递
const handleProgressiveStep1Confirm = async (data?: { extracted_tasks?: any[] }) => {
  if (data?.extracted_tasks) {
    console.log('✅ Step 1 - 用户修改了核心任务:', data.extracted_tasks);
    await api.resumeAnalysis(sessionId, { extracted_tasks: data.extracted_tasks });
  } else {
    console.log('✅ Step 1 - 用户确认核心任务');
    await api.resumeAnalysis(sessionId, { action: 'confirm' });
  }
};
```

---

## 🎨 UI/UX亮点

### 1. 任务卡片设计

**查看模式**:
```
┌────────────────────────────────────────────────┐
│  [1] 任务标题                         [编辑] [删除] │
│      任务描述...                               │
│      [关键词1] [关键词2]                        │
└────────────────────────────────────────────────┘
```

**编辑模式**:
```
┌────────────────────────────────────────────────┐
│  [1] 任务标题 * (至少5个字符)                    │
│      [输入框] 5/5                              │
│                                                │
│      任务描述 * (至少20个字符)                   │
│      [文本框]                                   │
│      20/20                                     │
│                                                │
│      [保存] [取消]                              │
└────────────────────────────────────────────────┘
```

**新增任务标记**:
```
┌│──────────────────────────────────────────────┐
││  [4] 新标题               [新增]  [编辑] [删除] │
││      新任务描述...                              │
│└──────────────────────────────────────────────┘
  └─绿色竖线
```

### 2. 添加按钮
```
┌────────────────────────────────────────────────┐
│  ┊ + 添加新任务 ┊                               │
└────────────────────────────────────────────────┘
虚线边框，达到15个时禁用并显示"（已达上限）"
```

### 3. 状态栏
```
┌────────────────────────────────────────────────┐
│ 原始任务数: 3  当前任务数: 5  新增: 2  删除: 0   │
│                            可添加 10 个任务      │
└────────────────────────────────────────────────┘
```

---

## 🔄 数据流

### 完整流程：

1. **初始化** (组件挂载)
   ```
   localStorage草稿 → editedTasks → 渲染任务列表
         ↓
   step1Data.extracted_tasks (无缓存时)
   ```

2. **用户编辑**
   ```
   用户操作 → updateTaskField → editedTasks更新
                                     ↓
                              自动保存到localStorage
   ```

3. **提交确认**
   ```
   handleConfirm → 检测修改 → onStep1Confirm({ extracted_tasks })
                                     ↓
                  handleProgressiveStep1Confirm(data)
                                     ↓
                  api.resumeAnalysis(sessionId, { extracted_tasks })
                                     ↓
                  后端step1_core_task节点接收
                                     ↓
                  state.user_modified_tasks = True
                  state.confirmed_core_tasks = 用户修改的任务
   ```

---

## 🛡️ 边界处理

### 1. 任务数量限制
- **最少**: 1个任务（删除时检查）
- **最多**: 15个任务（新增时检查）

### 2. 字段验证
- **标题**: 最少5个字符，实时显示字数
- **描述**: 最少20个字符，实时显示字数
- **保存按钮**: 未满足条件时禁用

### 3. 编辑冲突
- 检查是否有任务正在编辑（`isEditing: true`）
- 提示用户完成编辑后再确认

### 4. 草稿缓存
- 1小时有效期，过期自动清除
- 提交成功后清除缓存
- 缓存键包含sessionId，多会话隔离

### 5. 新任务处理
- 未完成的新任务（isEditing+isNew）在确认时自动过滤
- 取消编辑时直接删除未完成的新任务

---

## 🎯 用户体验优化

### 1. 平滑交互
- 新增任务后自动滚动到底部
- 编辑按钮hover显示，不干扰视觉
- 保存/取消按钮清晰可见

### 2. 即时反馈
- 字数实时统计
- 保存按钮根据验证状态启用/禁用
- 修改统计实时更新（状态栏+确认按钮）

### 3. 防误操作
- 删除前弹窗确认
- 最少保留1个任务
- 最多添加15个任务

### 4. 视觉引导
- 新增任务绿色标记
- 编辑中任务蓝色聚焦环
- 必填字段红色星号

---

## 📝 技术细节

### 1. TypeScript类型安全
```typescript
interface EditableTask {
  id?: string;
  title: string;
  description: string;
  source_keywords?: string[];
  isEditing?: boolean;
  isNew?: boolean;
}

interface UnifiedProgressiveQuestionnaireModalProps {
  onStep1Confirm: (data?: { extracted_tasks?: EditableTask[] }) => void;
  // ...
}
```

### 2. localStorage缓存格式
```json
{
  "tasks": [
    {
      "id": "task-1",
      "title": "任务标题",
      "description": "任务描述",
      "source_keywords": ["关键词1", "关键词2"],
      "isEditing": false,
      "isNew": false
    }
  ],
  "timestamp": 1704067200000
}
```

### 3. 后端State字段
```python
state = {
    "extracted_core_tasks": [...],        # AI原始识别任务
    "confirmed_core_tasks": [...],        # 用户确认/修改任务
    "user_modified_tasks": True/False,    # 🆕 v7.111: 修改标志
    "core_task_summary": "任务摘要",
    ...
}
```

---

## 🚀 待实施功能

### 1. Project Director识别修改标志 (P0)
**文件**: `intelligent_project_analyzer/agents/project_director.py`

**目标**: 在system prompt中检测`user_modified_tasks`标志，提示LLM优先满足用户自定义任务。

**实施方案**:
```python
# 在构建system prompt时
if state.get("user_modified_tasks"):
    system_prompt += """

⚠️ 用户已手动修改了核心任务列表，请严格按照用户自定义的任务进行分析，
不要偏离用户明确指定的任务范围。
"""
```

### 2. 后续增强 (P1)

**搜索策略和概念图配置** (已有执行计划):
- 在任务审批环节明确搜索指令
- 显示概念图方向和张数配置
- 允许用户微调搜索参数

---

## 📦 影响范围

### 修改文件
1. `frontend-nextjs/components/UnifiedProgressiveQuestionnaireModal.tsx` (+300行)
2. `intelligent_project_analyzer/interaction/nodes/progressive_questionnaire.py` (+10行)
3. `frontend-nextjs/app/analysis/[sessionId]/page.tsx` (+10行)

### 新增依赖
无

### 兼容性
- ✅ 完全向后兼容
- ✅ 旧格式响应依然支持
- ✅ 无Breaking Changes

---

## 🧪 测试建议

### 1. 功能测试
- [ ] 编辑任务标题和描述
- [ ] 保存/取消编辑
- [ ] 删除任务（边界：最少1个）
- [ ] 新增任务（边界：最多15个）
- [ ] 字数验证（标题5字，描述20字）
- [ ] 草稿缓存恢复（刷新页面）

### 2. 数据流测试
- [ ] 前端→后端任务数据传递
- [ ] 后端user_modified_tasks标志设置
- [ ] 草稿缓存过期清理

### 3. 边界测试
- [ ] 删除所有任务（应阻止）
- [ ] 添加超过15个任务（应阻止）
- [ ] 标题<5字保存（应禁用按钮）
- [ ] 描述<20字保存（应禁用按钮）
- [ ] 编辑中点击确认（应提示完成编辑）

### 4. 兼容性测试
- [ ] 旧格式响应（action: 'confirm'）
- [ ] 新格式响应（extracted_tasks）
- [ ] 跳过问卷流程

---

## 📊 版本演进

| 版本 | 功能 | 文档 |
|------|------|------|
| v7.105 | 统一三步问卷组件 | - |
| v7.109 | UI样式统一（CSS变量） | QUESTIONNAIRE_UI_UNIFIED_v7.109.md |
| v7.110 | 必填/选填UI增强 | QUESTIONNAIRE_REQUIRED_OPTIONAL_UI_v7.110.md |
| **v7.111** | **核心任务编辑功能** | **本文档** |

---

## 🎉 总结

### 实现目标
✅ 用户可修改、删除、新增核心任务
✅ 字数验证、数量限制
✅ 草稿缓存（自动保存+恢复）
✅ 后端识别用户修改标志
✅ 完全向后兼容

### 用户价值
- **自主权**: 用户可完全掌控核心任务列表
- **灵活性**: 支持增删改，适应多样化需求
- **安全性**: 数量/字数限制，防止误操作
- **便捷性**: 草稿缓存，避免意外丢失编辑

### 技术亮点
- **类型安全**: TypeScript严格类型检查
- **状态管理**: 清晰的编辑状态流转
- **缓存策略**: localStorage + 过期机制
- **平滑体验**: 内联编辑、实时验证、动态反馈

---

**实施时间**: 2024年 (版本v7.111)
**实施人员**: GitHub Copilot (Claude Sonnet 4.5)
**相关文档**: QUESTIONNAIRE_UI_UNIFIED_v7.109.md, QUESTIONNAIRE_REQUIRED_OPTIONAL_UI_v7.110.md
