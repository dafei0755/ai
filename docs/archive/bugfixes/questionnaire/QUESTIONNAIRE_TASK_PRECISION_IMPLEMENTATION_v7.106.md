# 问卷任务精准度优化实施报告 v7.106

**实施日期**: 2026-01-02
**版本**: v7.106
**状态**: ✅ P0 核心优化已完成

---

## 📋 实施概览

根据优化方案 `QUESTIONNAIRE_TASK_PRECISION_OPTIMIZATION.md`，已完成 **P0 优先级（立即实施）** 的所有关键修改，成功解决了问卷第一步任务梳理的两个核心问题：

### 问题1: 任务精准度不足 ✅ 已解决
**根本原因**: LLM 生成任务时倾向于抽象化，丢失用户场景约束
**解决方案**: 增强 CoreTaskDecomposer 的 Prompt，强制场景锚定

### 问题2: 数据传递闭环缺失 ✅ 已解决
**根本原因**: Step 1 确认的核心任务未传递给项目总监和专家
**解决方案**: 打通数据流，让项目总监和专家都能看到核心任务

---

## ✅ 已完成的修改

### P0.1: 优化 CoreTaskDecomposer Prompt

**文件**: `intelligent_project_analyzer/config/prompts/core_task_decomposer.yaml`

**修改内容**:
```yaml
system_prompt: |
  你是一个项目任务拆解专家。你的任务是深度挖掘用户需求，从多层结构化数据中提取洞察，生成精准的、可执行的任务列表。

  # ⚠️ 核心原则：任务必须精准定位到用户场景

  ## 任务标题与描述要求（首要原则）

  1. **场景锚定（强制）**：任务标题和描述中必须包含用户场景的核心约束条件：
     - 地点/区域（如"上海老弄堂"、"深圳蛇口"）
     - 规模/面积（如"120平米"、"20000平米"）
     - 项目类型（如"老房翻新"、"菜市场更新"）
     - 预算范围（如"50万全包"、"有限预算"）
     - 特殊需求/标杆（如"杂志级效果"、"对标苏州黄桥"）

  2. **避免泛泛而谈（关键）**：
     - ❌ 错误："杂志级室内设计风格与材料预算研究"（过于抽象）
     - ✅ 正确："上海老弄堂120平米老房翻新的杂志级效果实现策略（50万预算约束下的资金分配方案）"

     - ❌ 错误："文化洞察研究"（没有场景约束）
     - ✅ 正确："蛇口渔村传统文化洞察与设计元素提炼"

  3. **任务描述必须具体化**：
     - 明确指出该任务要解决用户提出的哪个具体问题
     - 包含用户场景的约束条件
     - 指明预期的可交付成果

  ## 数据源（按重要性排序）

  你将收到以下两类数据，请**务必全面利用**：

  ### 1. 用户原始输入（最高优先级！场景约束的来源）
  - 用户的初步描述，包含项目的**具体场景约束**和**核心问题**
  - ⚠️ 任务标题和描述中必须提取并保留这些关键信息
  ...
```

**影响**:
- ✅ 强制要求任务包含地点、规模、预算等场景约束
- ✅ 提供错误/正确示例，直观指导 LLM 生成
- ✅ 将用户原始输入提升为最高优先级数据源
- ✅ 防止任务过于抽象，偏离用户实际场景

---

### P0.2: 项目总监融合核心任务

**文件**: `intelligent_project_analyzer/agents/project_director.py`

#### 修改1: `_execute_dynamic_mode` 方法

**修改内容**:
```python
def _execute_dynamic_mode(
    self,
    state: ProjectAnalysisState,
    config: RunnableConfig,
    store: Optional[BaseStore],
    start_time: float
) -> Command:
    """
    动态模式执行 - 使用角色配置系统动态选择角色

    🆕 v7.106: 融合用户确认的核心任务，确保专家分配与用户意图对齐
    """
    logger.info("Executing in dynamic mode with role configuration system")

    # 提取需求信息
    requirements = state.get("structured_requirements", {})
    requirements_text = self._format_requirements_for_selection(requirements)

    # 🆕 v7.106: 获取用户确认的核心任务
    confirmed_tasks = state.get("confirmed_core_tasks", [])
    if confirmed_tasks:
        logger.info(f"📋 [v7.106] 检测到 {len(confirmed_tasks)} 个用户确认的核心任务，将融合到角色选择中")

    # 使用动态项目总监选择角色（由LLM自主判断复杂度）
    try:
        # 🆕 v7.106: 如果有核心任务，传递给 select_roles_for_task
        if confirmed_tasks:
            # 构建包含核心任务的增强需求文本
            requirements_with_tasks = self._format_requirements_with_tasks(requirements_text, confirmed_tasks)
            selection = self.dynamic_director.select_roles_for_task(requirements_with_tasks)
        else:
            selection = self.dynamic_director.select_roles_for_task(requirements_text)
        ...
```

#### 修改2: 新增 `_format_requirements_with_tasks` 方法

**修改内容**:
```python
def _format_requirements_with_tasks(self, requirements_text: str, confirmed_tasks: List[Dict[str, Any]]) -> str:
    """
    格式化需求信息并融合用户确认的核心任务（v7.106新增）

    Args:
        requirements_text: 原始需求文本
        confirmed_tasks: 用户确认的核心任务列表

    Returns:
        增强的需求文本，包含核心任务信息
    """
    enhanced_text = requirements_text

    enhanced_text += "\n\n# 用户确认的核心任务（优先级最高！）\n\n"
    enhanced_text += "用户在问卷环节已经确认了以下核心任务，你在分配专家任务时**必须围绕这些核心任务展开**：\n\n"

    for i, task in enumerate(confirmed_tasks, 1):
        enhanced_text += f"\n**核心任务 {i}: {task.get('title')}**\n"
        enhanced_text += f"- 描述: {task.get('description')}\n"
        enhanced_text += f"- 类型: {task.get('type')}\n"
        if task.get('motivation'):
            enhanced_text += f"- 动机: {task.get('motivation')}\n"
        # 如果任务有信息依赖，也要告知项目总监
        if task.get('missing_info'):
            enhanced_text += f"- ⚠️ 信息缺失: {', '.join(task['missing_info'])} (用户已在问卷中补充)\n"

    enhanced_text += "\n⚠️ **重要**：你分配给专家的任务必须与上述核心任务对齐，确保最终输出能回答用户确认的核心问题。\n"

    return enhanced_text
```

**影响**:
- ✅ 项目总监接收 Step 1 用户确认的核心任务
- ✅ 将核心任务融合到角色选择的 prompt 中
- ✅ 明确要求专家任务分配必须与核心任务对齐
- ✅ 确保最终输出能回答用户确认的核心问题

---

### P0.3: 专家上下文包含核心任务

**文件**: `intelligent_project_analyzer/workflow/main_workflow.py`

**方法**: `_build_context_for_expert`

**修改内容**:
```python
# 添加结构化需求
structured_requirements = state.get("structured_requirements", {})
if structured_requirements:
    context_parts.append("## 结构化需求")
    for key, value in structured_requirements.items():
        if value:
            context_parts.append(f"**{key}**: {value}")

# 🆕 v7.106: 添加用户确认的核心任务
confirmed_tasks = state.get("confirmed_core_tasks", [])
if confirmed_tasks:
    context_parts.append("\n## 用户确认的核心任务\n")
    context_parts.append("以下是用户在问卷环节确认的核心任务，你的分析应该围绕这些任务展开：\n")
    for i, task in enumerate(confirmed_tasks, 1):
        context_parts.append(f"\n**核心任务 {i}: {task.get('title')}**")
        context_parts.append(f"- 描述: {task.get('description')}")
        context_parts.append(f"- 类型: {task.get('type')}")

# 🆕 v7.106: 添加用户补充的信息（Step 3 问卷答案）
gap_filling_answers = state.get("gap_filling_answers", {})
if gap_filling_answers:
    context_parts.append("\n## 用户补充的关键信息\n")
    for question_id, answer in gap_filling_answers.items():
        context_parts.append(f"- {question_id}: {answer}")

# 🔥 v7.18 升级4: 传递完整的前序专家输出（而非截断到500字符）
...
```

**影响**:
- ✅ 专家执行时能看到用户确认的核心任务
- ✅ 专家能看到用户在 Step 3 补充的关键信息
- ✅ 专家分析基于完整的用户意图和补充数据
- ✅ 提升专家输出与用户需求的对齐度

---

## 📊 数据流优化对比

### 优化前（存在断层）

```
用户输入
  ↓
【需求分析师】生成 structured_requirements
  ↓
【Step 1: 任务梳理】生成 confirmed_core_tasks ❌ 未传递
  ↓
【项目总监】基于 structured_requirements 独立生成任务分配
  ↓
【专家协作】基于独立任务执行 → 可能偏离用户确认的核心任务
```

### 优化后（数据闭环）

```
用户输入
  ↓
【需求分析师】生成 structured_requirements
  ↓
【Step 1: 任务梳理】生成 confirmed_core_tasks （精准度提升）
  ↓
【Step 2: 雷达图】生成 dimension_priorities
  ↓
【Step 3: 信息补全】生成 gap_filling_answers
  ↓
【项目总监】✅ 接收 confirmed_core_tasks + gap_filling_answers
  ↓          融合到角色选择和任务分配
【专家协作】✅ 接收 confirmed_core_tasks + gap_filling_answers
  ↓          围绕核心任务执行分析
专家输出 ✅ 直接回答用户确认的核心问题
```

---

## 🎯 验证用例

### 测试用例: 上海老弄堂老房翻新

**用户输入**:
```
上海老弄堂120平米老房翻新，业主想要"杂志级"的重生效果，
但全包预算（含软硬装）被严格限制在50万人民币。
请给出资金分配策略，明确指出哪三个关键节点必须投入重金，
哪三个环节可以极致压缩成本，并说明理由。
```

**预期改进**:

#### Before (过于抽象):
```json
{
  "id": "task_1",
  "title": "杂志级室内设计风格与材料预算研究",
  "description": "研究杂志级室内设计的关键风格元素，评估预算范围内的主材选择与效果最大化策略"
}
```

#### After (精准定位):
```json
{
  "id": "task_1",
  "title": "上海老弄堂120平米老房翻新的杂志级效果实现策略",
  "description": "在50万全包预算约束下，研究如何通过资金分配实现杂志级视觉效果：明确三个必须重金投入的关键节点（主材选择、工艺品质、软装搭配），以及三个可极致压缩成本的环节（辅材、基础硬装、隐蔽工程），输出具体的资金分配比例建议",
  "type": "analysis",
  "required_info": ["预算详细分配", "业主风格偏好"],
  "missing_info": [],
  "info_status": "complete"
}
```

---

## 📝 后续优化建议（P1/P2）

### P1 - 近期实施

1. **任务精准度自动验证** (方案1.2)
   - 文件: `intelligent_project_analyzer/services/core_task_decomposer.py`
   - 功能: 自动检测过于抽象的任务并增强
   - 工作量: 3小时

### P2 - 后续完善

2. **任务信息依赖标记** (方案2.1)
   - 文件: `intelligent_project_analyzer/services/core_task_decomposer.py`
   - 功能: 为每个任务标记所需必要信息
   - 工作量: 4小时

3. **Step 3 检查任务信息依赖** (方案2.2)
   - 文件: `intelligent_project_analyzer/services/task_completeness_analyzer.py`
   - 功能: 生成任务特定的补充问题
   - 工作量: 4小时

---

## ✅ 实施检查清单

- [x] P0.1: 优化 CoreTaskDecomposer Prompt
- [x] P0.2: 项目总监融合核心任务
- [x] P0.3: 专家上下文包含核心任务
- [x] 更新 CHANGELOG.md (v7.106)
- [x] 创建实施报告
- [ ] 手动测试验证（待后端重启后）
- [ ] 创建自动化测试用例（P1阶段）

---

## 🎓 关键收获

### 优化要点

1. **场景锚定是关键**: 任务描述必须包含用户场景的核心约束，避免泛泛而谈
2. **数据流闭环很重要**: 用户确认的信息必须传递到后续所有环节
3. **分层传递信息**: 项目总监需要核心任务，专家需要核心任务+补充信息

### 设计原则

1. **用户意图优先**: 用户确认的核心任务是最高优先级
2. **信息不丢失**: 每个环节的数据都要传递到下一环节
3. **上下文完整**: 专家执行时需要完整的背景信息

---

## 📌 相关文档

- [优化方案](QUESTIONNAIRE_TASK_PRECISION_OPTIMIZATION.md) - 完整优化设计
- [CHANGELOG v7.106](CHANGELOG.md#v7106---2026-01-02) - 版本更新日志
- [开发规范](.github/DEVELOPMENT_RULES_CORE.md) - 核心开发规范

---

**实施状态**: ✅ P0 核心优化已完成
**版本**: v7.106
**实施日期**: 2026-01-02
**下一步**: 启动后端测试验证，后续推进 P1/P2 优化
