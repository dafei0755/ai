# 🚨 程序出错了？立即恢复！

## 最快恢复方法（1分钟）

### 方法1：双击运行（最简单）⭐

```
双击运行：emergency_restore.bat
```

按提示操作即可！

### 方法2：命令行恢复（最快）

```bash
# 查看可用版本
git log --oneline -10

# 恢复到指定版本（替换hash）
git checkout 86fa933
```

### 方法3：一键命令恢复（复制粘贴）

```bash
# 恢复到昨天最后一个稳定版本
git checkout fdfb351
```

## 当前可恢复的版本

```
86fa933 - 最新版本（概念图修复）
f10435a - 撤销需求确认修复
ab9a197 - 前端不稳定分析
fdfb351 - ⭐ v7.107 前端完整版本（推荐）
a15344a - 测试自动化更新
b6d2728 - Pytest编码修复
```

## 创建桌面快捷方式

```powershell
# 右键以管理员身份运行PowerShell
PowerShell -ExecutionPolicy Bypass scripts\create_emergency_shortcut.ps1
```

然后双击桌面上的 "🚨紧急恢复-历史版本" 图标即可！

## 完整说明

详见：[EMERGENCY_RECOVERY.md](EMERGENCY_RECOVERY.md)
