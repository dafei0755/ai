# HISTORICAL TEST ARCHIVE
# Tests in this directory are NOT run in CI (excluded via norecursedirs in pytest.ini).
# They document historical behavior of removed flags, deleted modules, or superseded implementations.
